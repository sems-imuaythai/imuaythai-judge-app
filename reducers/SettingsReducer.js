const settingsInitialState = {
    ring: ''
}
const settings = (state = settingsInitialState, action) => {
    switch (action.type) {
        case '':
            return state
        default:
            return state
    }
}

export default settings;